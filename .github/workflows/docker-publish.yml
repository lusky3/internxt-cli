name: Check for new internxt-cli release and build Docker image

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:
    inputs:
      force_rebuild:
        description: 'Force rebuild even if no new release'
        required: false
        default: false
        type: boolean

permissions:
  contents: write
  packages: write
  security-events: write

jobs:
  check-new-release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Check and save latest release
        id: check_save_release
        run: |
          latest_release=$(curl -s https://api.github.com/repos/internxt/cli/releases/latest | jq -r .tag_name)
          
          max_retries=5
          retry_count=0

          while [[ $retry_count -lt $max_retries ]]; do
              latest_release=$(curl -s https://api.github.com/repos/internxt/cli/releases/latest | jq -r .tag_name)

              if [[ -z "$latest_release" || "$latest_release" == "null" ]]; then
                  echo "Version lookup failed. Retrying in 30 seconds... (Attempt $((retry_count + 1)) of $max_retries)"
                  sleep 30
                  retry_count=$((retry_count + 1))
              else
                  echo "Latest release version: $latest_release"
                  break
              fi
          done

          if [[ $retry_count -eq $max_retries ]]; then
              echo "Version lookup failed after $max_retries attempts. Please investigate."
              exit 1
          fi

          echo "latest_release=$latest_release" >> $GITHUB_ENV
          
          last_known_release=$(cat .last_release 2>/dev/null || echo "")
          echo "Our version is: $last_known_release"
          
          if [ "$latest_release" != "$last_known_release" ]; then
            echo "New release detected."
            echo "new_release=true" >> $GITHUB_ENV

            git config user.name "github-actions"
            git config user.email "github-actions@github.com"
            git pull --rebase
            echo $latest_release > .last_release
            git add .last_release
            git commit -m "Update latest release to $latest_release"
            git push
          elif [ "${{ inputs.force_rebuild }}" == "true" ]; then
            echo "Force rebuild requested."
            echo "new_release=true" >> $GITHUB_ENV
          else
            echo "No new release found."
            echo "new_release=false" >> $GITHUB_ENV
            exit 0
          fi

      - name: Set up QEMU
        if: env.new_release == 'true'
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        if: env.new_release == 'true'
        uses: docker/setup-buildx-action@v4

      - name: Log in to GitHub Container Registry
        if: env.new_release == 'true'
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Log in to Docker Hub
        if: env.new_release == 'true'
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_KEY }}

      - name: Build and push new Docker image
        if: env.new_release == 'true'
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository }}/internxt-cli:${{ env.latest_release }}
            ghcr.io/${{ github.repository }}/internxt-cli:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/internxt-cli:${{ env.latest_release }}
            ${{ secrets.DOCKERHUB_USERNAME }}/internxt-cli:latest
          platforms: linux/amd64,linux/arm64
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Run Trivy vulnerability scanner
        if: env.new_release == 'true'
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ghcr.io/${{ github.repository }}/internxt-cli:${{ env.latest_release }}
          format: 'sarif'
          output: 'trivy-results.sarif'

      - name: Upload Trivy results to GitHub Security
        if: env.new_release == 'true'
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: 'trivy-results.sarif'

      - name: Discord notification for new release
        if: env.new_release == 'true'
        uses: sarisia/actions-status-discord@v1
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK }}
          status: Success
          title: "✅ New internxt-cli release detected: ${{ env.latest_release }}"
          description: |
            Docker image built and pushed successfully.
            [View Run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})
          color: 0x00ff00
